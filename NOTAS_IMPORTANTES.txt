NOTAS IMPORTANTES - CHATBOT MANUAL
===================================

CONFIGURACIÓN CORRECTA DE IMPORTS:

✅ FORMA CORRECTA (RECOMENDADA):
from langchain_community.embeddings import HuggingFaceEmbeddings

❌ FORMA ANTIGUA (PROBLEMÁTICA):
from langchain_huggingface import HuggingFaceEmbeddings

ARCHIVOS QUE DEBEN USAR LA FORMA CORRECTA:

- src/rag_engine.py (línea 1)
- src/process_manual.py (línea 3)

PASOS CRÍTICOS:

1. SIEMPRE actualizar pip primero: python -m pip install --upgrade pip
2. Usar langchain_community.embeddings para mayor estabilidad
3. El manual debe estar en data/manual.pdf
4. La API Key de Groq debe estar en archivo .env

VERIFICACIÓN RÁPIDA:

python -c "from langchain_community.embeddings import HuggingFaceEmbeddings; print('✅ Configuración correcta')"

SOLUCIÓN RÁPIDA DE PROBLEMAS:

# Reinstalación completa
deactivate
Remove-Item -Recurse -Force venv
python -m venv venv
.\venv\Scripts\activate
python -m pip install --upgrade pip
pip install -r requirements.txt